# Como executar

## Compilar e executar

No terminal, execute:

```bash
make
```

O Makefile irá compilar o projeto e executar automaticamente o programa.

## Limpar arquivos gerados

Para remover os arquivos de compilação:

```bash
make clean
```

